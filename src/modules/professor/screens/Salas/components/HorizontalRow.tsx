import { View } from "react-native"

const HorizontalRow = () => {
    return (
        <View style={{ borderBottomColor: '#DBDBDB', borderBottomWidth: 0.5, marginVertical: 10 }} />
    )
}

export default HorizontalRow;